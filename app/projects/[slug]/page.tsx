import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { projects } from "../../../data/projects";
import { CaseStudyHeader } from "../../../components/projects/CaseStudyHeader";
import { CallToAction } from "../../../components/ui/CallToAction";
export function generateStaticParams() { return projects.map(p => ({ slug: p.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const p = projects.find(item => item.slug === slug); return { title: p?.title ?? "Proyecto" }; }
export default async function ProjectPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const project = projects.find(p => p.slug === slug); if (!project) notFound(); const study = project.caseStudy; return <main><CaseStudyHeader project={project} />{study ? <div className="page-shell case-body"><aside className="case-nav">Caso de estudio<br />{project.title}</aside><div className="case-content"><section><h2>Contexto</h2><p>{study.context}</p></section><section><h2>Problema</h2><p>{study.problem}</p></section><section><h2>Objetivos</h2><ul>{study.objectives.map(item => <li key={item}>{item}</li>)}</ul></section><section><h2>Proceso</h2><ul>{study.process.map(item => <li key={item}>{item}</li>)}</ul></section><section><h2>Decisiones</h2><ul>{study.decisions.map(item => <li key={item}>{item}</li>)}</ul></section><section><h2>Herramientas</h2><div className="case-tools">{project.tools.map(tool => <span key={tool}>{tool}</span>)}</div></section><section><h2>Resultado</h2><p>{study.result}</p><p className="body-copy">La geolocalización depende de que el usuario comparta voluntariamente su ubicación.</p></section><section><h2>Evidencias</h2><ul>{study.evidence.map(item => <li key={item}>{item}</li>)}</ul></section><section><h2>Aprendizajes</h2><ul>{study.learnings.map(item => <li key={item}>{item}</li>)}</ul></section></div></div> : <div className="page-shell section"><p className="large-copy">Este proyecto está en proceso de documentación. El caso de estudio completo estará disponible próximamente.</p></div>}<div className="page-shell"><CallToAction title="¿Tienes una pregunta o un proyecto en mente?" text="Podemos conversar sobre educación digital, IA, productos y narrativas visuales." /></div></main>; }
