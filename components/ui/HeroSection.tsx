import Link from "next/link";
import { ArrowRight } from "lucide-react";
export function HeroSection({ eyebrow, title, text, primaryHref = "/projects", primary = "Explorar proyectos", secondaryHref = "/docencia-ia", secondary = "Conocer mi trabajo con IA", visual }: { eyebrow: string; title: string; text: string; primaryHref?: string; primary?: string; secondaryHref?: string; secondary?: string; visual?: React.ReactNode }) { return <section className="hero page-shell"><div className="hero-copy"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="hero-text">{text}</p><div className="hero-actions"><Link className="button" href={primaryHref}>{primary}<ArrowRight size={16} /></Link><Link className="text-link" href={secondaryHref}>{secondary} <ArrowRight size={15} /></Link></div></div>{visual}</section>; }
