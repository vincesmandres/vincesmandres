export function AbstractVisual({ variant, large = false }: { variant: string; large?: boolean }) { return <div className={`abstract-visual visual-${variant} ${large ? "visual-large" : ""}`} aria-label="Placeholder visual abstracto editable"><span className="visual-label">MAVM / {variant}</span><i className="shape shape-one"/><i className="shape shape-two"/><i className="shape shape-three"/><div className="visual-grid"/></div>; }
